extends Node3D

@onready var TruePlanetWorld = $CoreWorld/TruePlanetWorld
@onready var GenerateSystem = $System

# Called when the node enters the scene tree for the first time.
func _ready():
	pass

func _process(delta):
	#if Input.is_action_just_pressed("return"):
		#print("return")
		#get_tree().change_scene_to_file("res://Main/main.gd")
	pass
